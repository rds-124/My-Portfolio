import React from "react";

const SectionOverlay = () => {
  return (
    <div className="relative py-0 bg-transparent text-[hsl(var(--sidebar-foreground))]"/>
  ); //absolute inset-0 z-10 pointer-events-none bg-transparent dark:bg-black/70
};

export default SectionOverlay;


